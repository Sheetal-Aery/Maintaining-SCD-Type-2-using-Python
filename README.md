# Maintaining-SCD-Type-2-using-Python
This project contains a Python ETL pipeline that automates the process of handling product data. The code captures new product files in CSV format, loads them into SQL Server, and ensures that the database stays up to date.
